import React from "react";

const SupplementLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="min-h-full pt-20">{children}</div>;
};

export default SupplementLayout;
